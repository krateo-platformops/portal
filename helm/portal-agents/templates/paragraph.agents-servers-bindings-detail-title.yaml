{{/* Titles the pair list that sits UNDER the matrix, so the two are not read as the same table twice. */}}
{{/* */}}
{{/* `level: 4` — the matrix and this list are both answers to "Who may call what" (the h3 above them), */}}
{{/* so this is one step further down again: H1 page, h2 Tool servers, h3 Who may call what, h4 the */}}
{{/* detail view of it. The matrix needs no title of its own; it is what the h3 promises. */}}
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: agents-servers-bindings-detail-title
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: Every binding, with the tool names
    level: 4
  resourcesRefs:
    items: []
