apiVersion: v2
name: portal-agents
description: >-
  The agent control plane of the Krateo Composable Portal — /agents, agent detail, and the
  agent policies pages — as its own composition. Split out of the portal chart because a Helm
  release record is one Kubernetes Secret and a Secret is capped at 1 MiB: the portal crossed
  that ceiling on 2026-09-11 and silently stopped recording releases. Splitting gives this
  subsystem its own budget, so one feature section can no longer spend the whole portal.

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: CHART_VERSION

appVersion: CHART_VERSION
home: https://krateo.io
icon: https://raw.githubusercontent.com/krateo-platformops/.github/main/brand/logo.svg
keywords:
  - krateo
  - platformops
  - portal
  - agents
  - blueprint
  - composable
sources:
  - https://github.com/krateo-platformops/portal
annotations:
  krateo.io/category: application
  krateo.io/maturity: stable
  krateoSupportedVersion: ">= 2.5.1"
