# Sub-section title for the agent x server binding table.
#
# `level: 3` — this block is nested INSIDE Tool servers (flex.agents-servers composes both), so it
# sits one step under that section's h2 and two under the page H1. Previously `strong: true`, which
# rendered identically to both of them.
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: agents-servers-bindings-title
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: Who may call what
    level: 3
  resourcesRefs:
    items: []
