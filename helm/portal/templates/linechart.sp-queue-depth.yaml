{{- if (.Values.observability | default dict).snowplowSli }}
{{/* Work waiting. Sustained depth means the refresher is not keeping up. */}}
kind: LineChart
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: sp-queue-depth
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
    "krateo.io/external-cache-ttl-seconds": "30"
spec:
  keyExtras: [range]
  widgetData:
    xField: t
    yField: v
    colorField: series
    colorMap:
      "snowplow_refresher_queue_depth": cyan
      "snowplow_prewarm_engine_pending_depth": teal
    shapeField: smooth
    area: false
    legend: true
    height: 160
    xTimeUnit: hour
    point: false
    scale:
      "y":
        zero: true
        nice: true
        domainMin: 0
        tickCount: 4
    axis:
      x:
        tickCount: 6
      "y":
        tickCount: 4
    data: []
  widgetDataTemplate:
  - forPath: data
    expression: '${ [ (.rows // [])[] | { t: (.t|tonumber), series: (.series|tostring), v: (.v|tonumber) } ] }'
  apiRef:
    name: sp-queue-depth
    namespace: {{ .Release.Namespace }}
  resourcesRefs:
    items: []
{{- end }}
