kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: settings-subtitle
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    # Settings now hosts user administration only — the Krateo control-plane status
    # tab moved to Observability (Components tab).
    text: Manage local users, groups, and permissions.
    type: secondary
  resourcesRefs:
    items: []
