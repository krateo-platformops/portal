{{/* The incident meta line as a Descriptions list: Started, Analyzed, Firings, Last fired and */}}
{{/* how it ended, each shaped server-side by incident-detail (.metaItems, relative times) and */}}
{{/* omitted when unknown. */}}
kind: Descriptions
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: incident-meta-desc
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  {{/* F6 (snowplow#130): extras keys that partition this widget's cache cell. */}}
  keyExtras: [name, namespace]
  apiRef:
    name: incident-detail
    namespace: {{ .Release.Namespace }}
  widgetData:
    {{/* Label/value pairs inline under the title and chips, not a stacked table. */}}
    column: 3
    variant: default
    bordered: false
    size: small
    items: []
  widgetDataTemplate:
    - forPath: items
      expression: '${ .metaItems // [] }'
  resourcesRefs:
    items: []
