# Title for the /agents/policies pagehead.
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: agents-policies-title
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: What the gateway enforces
    strong: true
  resourcesRefs:
    items: []
