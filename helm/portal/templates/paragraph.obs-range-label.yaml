{{/* Eyebrow label for the observability time-range chip group. */}}
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: obs-range-label
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: 'Time range'
    variant: eyebrow
  resourcesRefs:
    items: []
