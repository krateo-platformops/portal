{{- if (.Values.observability | default dict).snowplowSli }}
kind: Card
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: sp-card-informer-freshness
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    title: Informer freshness
    titleVariant: eyebrow
    variant: outlined
    allowedResources:
    - linecharts
    items:
    - resourceRefId: sp-informer-freshness
  resourcesRefs:
    items:
      - id: sp-informer-freshness
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: sp-informer-freshness
        namespace: {{ .Release.Namespace }}
        resource: linecharts
        verb: GET
{{- end }}
