kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: blueprint-builder-subtitle
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: Describe a blueprint to Autopilot and it drafts the Helm chart, previews a live helm-render, and opens a gated pull/merge request to the blueprints repository. Once merged, it publishes an OCI chart and appears below — installable by anyone from the Blueprints page. Each row links to its blueprint.
    type: secondary
  resourcesRefs:
    items: []
