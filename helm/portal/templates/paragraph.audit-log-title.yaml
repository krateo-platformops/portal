# Section header for the Audit log on the Settings page (mirrors settings-title). Separates the
# audit trail from the Users table above it.
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: audit-log-title
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: Audit log
    strong: true
  resourcesRefs:
    items: []
