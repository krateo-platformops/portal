# Convention page root for /incidents/{namespace}/{name} — the investigation anatomy
# (mockup §C), strict top-to-bottom: header → EVIDENCE → MISSING CONTEXT/ASSUMPTIONS →
# REASONING trace → ROOT CAUSE → REMEDIATION plan → SIMILAR incidents → the v1 prose fallback.
#
# UX-20 (unified section language): every major section is now ONE antd Card of the same family
# (titleVariant: eyebrow + variant: outlined + a one-line headerLeft desc). Evidence, Reasoning
# trace and Remediation are the section-CARD widgets (incident-sec-*-card, defined in
# paragraphs.incident-section-labels) that WRAP their content (the analyzed Table + evidence
# Markdown, the Steps, the gated rem rows); Root cause and Missing context are their own Cards.
# The page now stacks these cards directly — the old "strong-label Paragraph + secondary-desc
# Paragraph + loose widget" triples are gone, so the detail reads as one visual language (incident
# first, RCA second) instead of a stack of different UIs.
#
# `items` are templated SERVER-SIDE off incident-detail's `sections` flags: every section
# renders ONLY when its structured data exists — a v1 prose-only report degrades to
# header + prose, and nothing ever renders as fabricated empty structure (exception-only,
# honest data). resourcesRefs stay static (the full pool); unreferenced children are
# simply never fetched.
kind: Flex
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: page-incident-detail
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  # F6 (snowplow#130): extras keys that partition this widget's cache cell.
  keyExtras: [name, namespace]
  apiRef:
    name: incident-detail
    namespace: {{ .Release.Namespace }}
  widgetData:
    allowedResources:
    - flexes
    - paragraphs
    - tables
    - markdowns
    - cards
    - steps
    - listies
    # #86 §0.7: buttons/tags stay permitted for backward-compat, but the "Related alerts" alert
    # link Button / inert Tag now render INSIDE incident-related-alerts-card (Card children aren't
    # constrained by this enum anyway), not as direct page children.
    - buttons
    - tags
    vertical: true
    gap: middle
    items:
    - resourceRefId: incident-detail-header-block
  widgetDataTemplate:
    # #72 §1 restructure: header → SUMMARY (TL;DR) → WHAT HAPPENED (evidence) → WHY (merged root
    # cause + gaps + trace, ONE card) → HOW TO FIX (remediation) → similar → prose. The old flat
    # gaps/trace/rootcause peer cards folded into incident-why-card; the Why card is included when
    # ANY of the three has data. Included ONLY when its structured data exists (sections flags); the
    # Cards fetch their own content children, so the page stacks section CARDS, not loose widgets.
    - forPath: items
      expression: >-
        ${ (.sections // {}) as $s
        | [ { resourceRefId: "incident-detail-header-block" } ]
        + (if ($s.summary // false) then [ { resourceRefId: "incident-summary-card" } ] else [] end)
        + (if ($s.evidence // false) then [ { resourceRefId: "incident-sec-evidence-card" } ] else [] end)
        + (if (($s.rootcause // false) or ($s.gaps // false) or ($s.trace // false)) then [ { resourceRefId: "incident-why-card" } ] else [] end)
        + (if ($s.remediation // false) then [ { resourceRefId: "incident-sec-remediation-card" } ] else [] end)
        + (if ($s.audit // false) then [ { resourceRefId: "incident-sec-audit-card" } ] else [] end)
        + (if ($s.similar // false) then [ { resourceRefId: "incident-similar-card" } ] else [] end)
        + (if (.alertName // "") != "" then [ { resourceRefId: "incident-related-alerts-card" } ] else [] end)
        + (if ($s.prose == true) or ($s == {}) then [ { resourceRefId: "incident-prose" } ] else [] end) }
  resourcesRefs:
    items:
    - id: incident-detail-header-block
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: incident-detail-header-block
      namespace: {{ .Release.Namespace }}
      resource: flexes
      verb: GET
    # Unified section cards — each wraps its own content widgets in its own resourcesRefs, so the
    # keyExtras walk still reaches every leaf transitively. #72 §1: Summary (TL;DR Descriptions) and
    # Why (merged root cause + gaps + trace) are new; the standalone gaps/trace/rootcause cards are
    # retired (folded into incident-why-card).
    - id: incident-summary-card
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: incident-summary-card
      namespace: {{ .Release.Namespace }}
      resource: cards
      verb: GET
    - id: incident-sec-evidence-card
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: incident-sec-evidence-card
      namespace: {{ .Release.Namespace }}
      resource: cards
      verb: GET
    - id: incident-why-card
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: incident-why-card
      namespace: {{ .Release.Namespace }}
      resource: cards
      verb: GET
    - id: incident-sec-remediation-card
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: incident-sec-remediation-card
      namespace: {{ .Release.Namespace }}
      resource: cards
      verb: GET
    # Feature #7: the Remediation audit section Card (wraps the audit Table; defined in
    # paragraphs.incident-section-labels). Gated on sections.audit in the items template above.
    - id: incident-sec-audit-card
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: incident-sec-audit-card
      namespace: {{ .Release.Namespace }}
      resource: cards
      verb: GET
    # #86 §0.7: Similar incidents is now a section Card (wraps the incident-similar Listy in its own
    # resourcesRefs, so the keyExtras walk still reaches it); the loose label + listy page refs are gone.
    - id: incident-similar-card
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: incident-similar-card
      namespace: {{ .Release.Namespace }}
      resource: cards
      verb: GET
    - id: incident-prose
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: incident-prose
      namespace: {{ .Release.Namespace }}
      resource: markdowns
      verb: GET
    # #86 §0.7: Related alerts is now a section Card (wraps the alert link/text Button/Tag in its own
    # resourcesRefs and makes the route-resolved link-vs-text choice itself); gated on .alertName in
    # the items template above.
    - id: incident-related-alerts-card
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: incident-related-alerts-card
      namespace: {{ .Release.Namespace }}
      resource: cards
      verb: GET
