# Left column (wider): Pods, Conditions, Events. #88 §0.4: Conditions moved here
# from the right column (Conditions reads as "why is it in this state," Events as
# "what happened," so Conditions leads before Events) — the right column keeps
# only Overview.
kind: Flex
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: component-detail-col-left
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  # F6 (snowplow#130): extras keys that partition this widget's cache cell.
  keyExtras: [name]
  widgetData:
    vertical: true
    gap: small
    allowedResources:
      - cards
    items:
      - resourceRefId: card-component-detail-conditions
      - resourceRefId: card-component-detail-pods
      - resourceRefId: card-component-detail-conditions
      - resourceRefId: card-component-detail-events
  resourcesRefs:
    items:
      - id: card-component-detail-conditions
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: card-component-detail-conditions
        namespace: {{ .Release.Namespace }}
        resource: cards
        verb: GET
      - id: card-component-detail-pods
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: card-component-detail-pods
        namespace: {{ .Release.Namespace }}
        resource: cards
        verb: GET
      - id: card-component-detail-conditions
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: card-component-detail-conditions
        namespace: {{ .Release.Namespace }}
        resource: cards
        verb: GET
      - id: card-component-detail-events
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: card-component-detail-events
        namespace: {{ .Release.Namespace }}
        resource: cards
        verb: GET
