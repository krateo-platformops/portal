{{/* Convention page root for /incidents — the SRE page (mockup §B): pagehead, lifecycle */}}
{{/* filter chips (live counts, server-side), the incident table (one row per */}}
{{/* TroubleshootingReport, row click → detail), and the exception-only "awaiting */}}
{{/* investigation" strip (firing alerts without a report; hidden when none). */}}
kind: Flex
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: page-incidents
  namespace: {{ include "portal.tierNamespace" (dict "ctx" . "tier" "admin") }}
  annotations:
    "krateo.io/nav-group": "Observability"
    "krateo.io/nav-icon": "fa-triangle-exclamation"
    "krateo.io/nav-label": "Incidents"
    "krateo.io/nav-order": "12"
    "krateo.io/nav-path": "/incidents"
    "krateo.io/verbose": "true"
spec:
  widgetData:
    allowedResources:
    - pageheaders
    - flexes
    - tables
    - listies
    vertical: true
    gap: middle
    items:
    - resourceRefId: incidents-page-header
    {{/* The lifecycle chips are ONE data-driven Listy(chip) — the old chips-only */}}
    {{/* incidents-toolbar Flex and its four looped Buttons are gone (widget-sprawl collapse). */}}
    - resourceRefId: incidents-status-chips
    {{/* PRIMARY: the incident table. */}}
    - resourceRefId: incidents-table
    - resourceRefId: incidents-awaiting
  resourcesRefs:
    items:
    - id: incidents-page-header
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: incidents-page-header
      namespace: {{ .Release.Namespace }}
      resource: pageheaders
      verb: GET
    - id: incidents-status-chips
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: incidents-status-chips
      namespace: {{ .Release.Namespace }}
      resource: listies
      verb: GET
    - id: incidents-table
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: incidents-table
      namespace: {{ .Release.Namespace }}
      resource: tables
      verb: GET
    - id: incidents-awaiting
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: incidents-awaiting
      namespace: {{ .Release.Namespace }}
      resource: listies
      verb: GET
