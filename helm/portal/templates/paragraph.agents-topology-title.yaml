# Section title for the gateway-topology block of /agents.
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: agents-topology-title
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: Every hop crosses one gateway
    strong: true
  resourcesRefs:
    items: []
