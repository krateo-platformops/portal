{{/* Observability section header — obs-sec-logs-label */}}
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: obs-sec-logs-label
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: 'Telemetry'
    strong: true
  resourcesRefs:
    items: []
