{{- if (.Values.observability | default dict).snowplowSli }}
kind: Statistic
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: sp-informer-states-synced
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
    "krateo.io/external-cache-ttl-seconds": "30"
spec:
  keyExtras: [range]
  apiRef:
    name: sp-informer-states
    namespace: {{ .Release.Namespace }}
  widgetData:
    title: Informers synced
    value: 0
  widgetDataTemplate:
  - forPath: value
    expression: '${ ([ (.rows // [])[] | select(.state=="synced") | (.latest|tonumber) ] | first // 0) }'
  resourcesRefs:
    items: []
{{- end }}
