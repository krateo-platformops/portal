{{/* /incidents data source — one row per Incident (incidents.observability.krateo.io, owned by */}}
{{/* incident-controller), shaped server-side: */}}
{{/* rows     : title (the alert's display name, else the root cause, else the alert name), */}}
{{/*            state, firings, age, severity dot colour; filtered by the ?status= chip (extras). */}}
{{/* counts   : per-state totals over the full set, so every chip shows its live count. */}}
{{/* awaiting : firing Alerts (state ALERT) with no open incident — the "not yet investigated" */}}
{{/*            strip, each with an /incidents?ask= Autopilot deep-link. */}}
{{/* projects : echo of the shell scope param, so the chips can carry it into their routes. */}}
{{/* An incident belongs to the alert named by its observability.krateo.io/alert label. */}}
apiVersion: templates.krateo.io/v1
kind: RESTAction
metadata:
  name: incidents-list
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  api:
    - name: incidents
      path: /apis/observability.krateo.io/v1alpha1/namespaces/{{ .Release.Namespace }}/incidents
      verb: GET
      continueOnError: true
      errorKey: incidentsError
    - name: alerts
      path: /apis/observability.krateo.io/v1alpha1/namespaces/{{ .Release.Namespace }}/alerts
      verb: GET
      continueOnError: true
      errorKey: alertsError
  filter: |
    def alertlabel: ((.metadata.labels // {})["observability.krateo.io/alert"] // .spec.alertRef.name // "");
    (.status // "all" | tostring | ascii_downcase) as $sel
    | ((.incidents.items) // []) as $items
    | ((.alerts.items) // []) as $alerts
    | ($alerts | map({ key: (.metadata.name // ""), value: (.spec.displayName // "") }) | from_entries) as $display
    | ($items | map(
        (.status.state // "Analyzing") as $state
        | alertlabel as $alert
        | (($display[$alert] // "") | gsub("[^ -~]"; "") | sub("^ +"; "") | sub(" +$"; "")) as $dn
        | {
            name: (.metadata.name // ""),
            # A namespaced list omits .metadata.namespace per item; these incidents are in the namespace just listed.
            namespace: "{{ .Release.Namespace }}",
            title: (if $dn != "" then $dn elif (.status.rootCause.statement // "") != "" then .status.rootCause.statement elif $alert != "" then $alert else (.metadata.name // "") end),
            state: $state,
            stateKey: ($state | ascii_downcase),
            firings: (.status.firings // 0),
            age: (.metadata.creationTimestamp // .spec.triggeredAt // ""),
            triggeredAt: (.spec.triggeredAt // .metadata.creationTimestamp // ""),
            sevColor: ({ Open: "#cf1322", Verifying: "#d48806", Resolved: "#389e0d" }[$state] // "#8c8c8c")
          }
      ) | sort_by(.triggeredAt) | reverse) as $rows
    | ([ $items[] | select((.status.state // "Analyzing") as $s | ($s != "Resolved" and $s != "Closed")) | alertlabel ] | unique) as $openAlerts
    | ([ $alerts[]
         | select((.status.state // "") == "ALERT")
         | select((.metadata.name // "") as $a | ($openAlerts | index($a)) == null)
         | { name: (.spec.displayName // .metadata.name // ""),
             state: (.status.state // "ALERT"),
             askHref: ("/incidents?ask=" + (("Investigate the \"" + (.spec.displayName // .metadata.name // "this alert") + "\" observability alert — it is firing but has no open incident. Run a root-cause analysis and give me the concrete next steps.") | @uri)) }
       ] | .[0:8]) as $awaiting
    | {
        rows: (if $sel == "all" then $rows else [ $rows[] | select(.stateKey == $sel) ] end),
        counts: ({ all: ($rows | length) }
          + ([ "analyzing", "open", "verifying", "resolved", "closed" ] | map({ key: ., value: (. as $k | [ $rows[] | select(.stateKey == $k) ] | length) }) | from_entries)),
        status: $sel,
        projects: ((.projects // "") | tostring),
        awaiting: $awaiting,
        awaitingCount: ($awaiting | length)
      }
