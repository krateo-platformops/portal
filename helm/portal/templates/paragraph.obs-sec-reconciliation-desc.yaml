{{/* Observability section header — obs-sec-reconciliation-desc */}}
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: obs-sec-reconciliation-desc
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: 'Composition health from live cluster snapshots — totals, drift, coverage and distribution.'
    type: secondary
  resourcesRefs:
    items: []
