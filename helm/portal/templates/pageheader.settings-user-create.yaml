{{/* The /settings/users/new page header. */}}
kind: PageHeader
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: settings-user-create-page-header
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    title: Add user
    subtitle: "Creates a portal account: a User resource plus the basic-auth Secret holding its password."
  resourcesRefs:
    items: []
