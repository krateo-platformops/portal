{{/* Observability → Components, second section header — obs-sec-pins-label */}}
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: obs-sec-pins-label
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: 'Declared versions'
    strong: true
  resourcesRefs:
    items: []
