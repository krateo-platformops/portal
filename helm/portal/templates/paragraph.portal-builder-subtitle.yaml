kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: portal-builder-subtitle
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: "Ask Autopilot to build a new page, blueprint, or API mapping. It previews the draft live, then opens a gated pull/merge request in the platform's git repos — on whichever SCM you publish to. Your in-flight builder change requests are listed below."
    type: secondary
  resourcesRefs:
    items: []
