kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: builderdemo-note
  namespace: krateo-system
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: "This page was authored by the Krateo Portal builder and published end-to-end via git-provider (previewPage -> a gated change request to krateo-portal-chart -> merge -> the portal composition re-rendered these widget CRs). Nothing was hand-edited on the cluster."
    type: secondary
  resourcesRefs:
    items: []
