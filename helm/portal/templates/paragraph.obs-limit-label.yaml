# Eyebrow label for the log-stream row-limit chip group ("extend the base set" affordance).
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: obs-limit-label
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: 'Rows'
    variant: eyebrow
  resourcesRefs:
    items: []
