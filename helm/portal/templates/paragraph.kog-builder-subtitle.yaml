kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: kog-builder-subtitle
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: One row per RestDefinition — OpenAPI mappings compiled into new controllers, with readiness and the generated kind straight from their status
    type: secondary
  resourcesRefs:
    items: []
