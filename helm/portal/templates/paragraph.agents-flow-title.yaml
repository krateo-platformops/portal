# Section title for the request-flow block of /agents.
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: agents-flow-title
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: One request, seven hops
    strong: true
  resourcesRefs:
    items: []
