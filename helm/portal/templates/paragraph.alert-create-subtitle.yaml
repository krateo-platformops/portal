kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: alert-create-subtitle
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: Define a HyperDX alert rule — what to watch, the threshold and the evaluation interval. Fields come from the live Alert CRD; a firing alert opens on the Incidents page and auto-triages in Autopilot.
    type: secondary
  resourcesRefs:
    items: []
