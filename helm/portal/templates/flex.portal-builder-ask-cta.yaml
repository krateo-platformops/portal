{{/* #84 §0.1: right-aligned holder for the /portal-builder "Ask Autopilot →" primary CTA. Button has */}}
{{/* no justify/alignment field of its own, so a flush-right Flex row (justify: flex-end) wrapping just */}}
{{/* the button gives the header CTA its right alignment. Mirrors flex.obs-troubleshooting-cta. */}}
kind: Flex
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: portal-builder-ask-cta
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    allowedResources:
    - buttons
    justify: flex-end
    items:
    - resourceRefId: portal-builder-ask
  resourcesRefs:
    items:
    - id: portal-builder-ask
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: portal-builder-ask
      namespace: {{ .Release.Namespace }}
      resource: buttons
      verb: GET
