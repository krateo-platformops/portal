{{/* Observability section header — obs-sec-logs-desc */}}
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: obs-sec-logs-desc
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: 'Error logs, live tail, and resource usage — from the OTel → ClickHouse pipeline.'
    type: secondary
  resourcesRefs:
    items: []
