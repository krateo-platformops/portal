# Observability section header — obs-sec-components-desc
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: obs-sec-components-desc
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: "Krateo's own control-plane Deployments — live readiness and image, current cluster state (the time range doesn't apply here)."
    type: secondary
  resourcesRefs:
    items: []
