{{- if (.Values.observability | default dict).snowplowSli }}
kind: Card
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: sp-card-fallthrough
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    title: Fall-through vs diagnostic
    titleVariant: eyebrow
    variant: outlined
    allowedResources:
    - linecharts
    items:
    - resourceRefId: sp-fallthrough
  resourcesRefs:
    items:
      - id: sp-fallthrough
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: sp-fallthrough
        namespace: {{ .Release.Namespace }}
        resource: linecharts
        verb: GET
{{- end }}
