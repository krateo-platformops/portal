{{- if (.Values.observability | default dict).snowplowSli }}
kind: Card
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: sp-card-dep-evictions
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    title: Dep-tracker evictions by route
    titleVariant: eyebrow
    variant: outlined
    allowedResources:
    - linecharts
    items:
    - resourceRefId: sp-dep-evictions
  resourcesRefs:
    items:
      - id: sp-dep-evictions
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: sp-dep-evictions
        namespace: {{ .Release.Namespace }}
        resource: linecharts
        verb: GET
{{- end }}
